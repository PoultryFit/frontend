# Frontend 2 — Dashboard Feature Modules

Ownership: Feasibility, Feed Plan, Find Help, Health Triage and their feature-specific hooks/data dependencies.

The module source files were copied from the original project without rewriting their contents. This repo is the feature-work workspace; Frontend 1 owns the app shell and auth routes.

## Integration
Bring `src/components/modules/*`, the feature route files and their required hook/lib files into the final frontend repository. Keep API endpoints owned by Backend 1/2/3 behind environment-configured service URLs.
